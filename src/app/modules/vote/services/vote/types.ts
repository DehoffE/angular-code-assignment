export type PollCard = {
  title: string;
  categoryName: string;
  categoryId: number;
  votersCount: number;
  points: number;
  bgColor: string;
  textColor: string;
  imageUrl: string;
  iconUrl: string;
}
